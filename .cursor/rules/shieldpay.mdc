---
description: ShieldPay module boundaries, contracts, and agent workflow
alwaysApply: true
---

# ShieldPay

Read `AGENTS.md` first. Summary:

- **P1** `lib/core/`, `lib/adapters/shopify/`, `app/api/core/`
- **P2** `lib/p2/`, `app/api/p2/`
- **P3** `lib/p3/`, `app/api/p3/`
- **P4** `lib/p4/`, `app/api/p4/`, `app/dashboard/`

Never cross-import modules. Use `shared/schemas.ts` (your section only) + frozen HTTP routes in AGENTS.md.

Before finishing: `npm run build` && `npm run test:px`.
